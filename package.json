{
  "name": "elm-decode-runner",
  "version": "0.1.0",
  "description": "CLI tool to run an Elm decoder on stdin JSON",
  "main": "./cli.js",
  "bin": {
    "elm-decode-runner": "cli.js"
  },
  "scripts": {
    "test": "BATS_TEST_DIRNAME=$(pwd)/tests ./tests/decoding.bats"
  },
  "dependencies": {
    "node-elm-compiler": "^5.0.4"
  },
  "devDependencies": {
    "elm": "^0.19.1-6"
  },
  "keywords": [
    "elm",
    "decoder",
    "cli",
    "json"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-username/elm-decode-cli.git"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=10.0.0"
  }
}
